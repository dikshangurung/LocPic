export class Place {
	constructor(title, imageUri, rating, description, id) {
		this.title = title;
		this.imageUri = imageUri;
		this.description = description;
		this.rating = rating;
		this.id = id;
	}
}
